// convention.js — 교안 컨벤션 규칙 뷰어

function getConventionMarkdown() {
    return `## 현재 적용된 교안 마크다운 문법 규칙 (전체)

---

### 1. 헤딩 위계

| 레벨 | 형식 | 예시 |
|:---:|:---|:---|
| H1 | \`# 📝 차시제목\` | 탭 시작부 1회만 |
| H2 | \`## 주요 개념\` | 넘버링 금지 |
| H3 | \`### ■ 세부 개념\` | ■ 접두사 |
| H4 | \`#### ▣ 하위 개념\` | ▣ + \`&nbsp;&nbsp;\` |

---

### 2. 이모지 규칙 (최우선)

| 규칙 | 설명 |
|:---|:---|
| 이모지 단독 사용 | 이모지는 항상 줄 앞에 단독으로 위치 |
| 넘버링+이모지 금지 | \`1. 💡\` ❌ → \`💡\` ✅ |
| 이모지+라벨 금지 | \`💡 이론 설명\` ❌ → \`💡 바로 본문\` ✅ |
| 학습 목표 섹션 이모지 금지 | \`## 학습 목표\` 하위에서 이모지 사용 ❌ |

**시맨틱 이모지:** 💡=핵심, ✅=체크, 🎮=게임사례, ⚠️=주의, ⚙️=실습, ☑️=강조

---

### 3. 넘버링/불릿 규칙 (최우선)

| 규칙 | 올바른 예 | 잘못된 예 |
|:---|:---|:---|
| 이모지/볼드 제목 아래는 불릿 | \`💡 **제목:**\` → \`  - 설명\` | \`1. 제목\` → \`2. 설명\` |
| 들여쓰기 넘버링 → 불릿 | \`  - 하위 항목\` | \`  1. 하위 항목\` |
| 넘버링 사용 제한 | 절차/단계에서만 허용 | 나열/설명에 넘버링 ❌ |

**후처리 동작:**
- \`extractText()\`: 이모지 줄 뒤 넘버링 → \`  - \` 불릿 자동 변환
- \`postprocessHtml()\`: \`<ol>\` 내 이모지 항목 → \`<ul>\` 변환

---

### 4. 텍스트 서식

| 서식 | 용도 | 예시 |
|:---|:---|:---|
| **볼드** | 개념/키워드 강조 | \`**플레이어 경험**\` |
| \`인라인코드\` | 파일명/단축키/코드 | \`Ctrl+Z\` |
| 불릿 \`-\` | 기본 설명 (Depth 1) | \`- 설명\` |
| 들여쓰기 불릿 \`  -\` | 하위 상세 (Depth 2) | \`  - 상세\` |
| 깊은 들여쓰기 \`    -\` | 추가 세부 (Depth 3) | \`    - 세부\` |

---

### 5. 간격 규칙

| 상황 | 간격 |
|:---|:---:|
| 헤딩 + 본문 | 0줄 (붙임) |
| 단락 ↔ 단락 | 1줄 (\`<br>\` 1개) |
| 텍스트 ↔ 블록(표/이미지) | 1줄 (\`<br>\` 1개) |
| 블록 ↔ 블록 | 2줄 (\`<br><br>\`) |
| 차시 전환 | \`<br><br>\` + \`---\` + \`<br>\` |

---

### 6. 표 규칙

- **HTML \`<table>\` 인라인 스타일** 사용 (GFM 표 금지)
- \`th\`: \`background-color: #f2f2f2; text-align: center;\`
- \`td/th\`: \`border: 1px solid black; padding: 10px;\`
- 한국어 마침표 줄바꿈: **표 내부에서는 적용 안 됨**

**색상 규칙 (제출/상태 표):**

| 상태 | 배경색 | 코드 |
|:---|:---:|:---|
| 제출 / 완료 | 🟩 | \`background-color:#4CAF50; color:#fff\` |
| 미제출 / 미완료 | 🟥 | \`background-color:#f44336; color:#fff\` |
| 진행중 / 부분 | 🟨 | \`background-color:#FF9800; color:#fff\` |
| 면제 / 해당없음 | ⬜ | \`background-color:#9E9E9E; color:#fff\` |

---

### 7. 이미지 태그

- \`<!-- [IMG: "영어 키워드"] -->\` 형식만 허용
- \`![alt](url)\` 또는 \`<img src="local:...">\` 직접 출력 금지
- 학습 목표 섹션: 이미지 생성 제외

---

### 8. 강사 스크립트 (v8 — 읽기만 해도 수업 가능)

- 각 \`##\` 섹션 직후에 \`<div class="instructor-callout">\` 블록 삽입
- **모든 탭에서 포함** (학습이해도 탭 제외)
- Callout 내부 구조 (순서대로):
  - ⏱️ **예상 소요** / 🎚️ **난이도**
  - 🎬 **도입 멘트** (이 섹션 시작 시 말할 구어체 2~3문장)
  - 🗣️ **핵심 설명 대본** (본문 내용을 구어체로 풀어쓴 강의 스크립트 5~10문장)
  - 💡 **꼭 짚어야 할 핵심** (불릿 2~3개)
  - ⚠️ **자주 헷갈리는 포인트** (오해→올바른 이해)
  - 🗣️ **질문/참여 유도** (개방형 질문 + 예상 답변)
  - 🔗 **전환 멘트** (다음 섹션 연결 1~2문장)
- 🎓 학생뷰로 전환 시 callout 자동 숨김

---

### 9. 톤 규칙

| 위치 | 톤 |
|:---|:---|
| 헤딩 | 체언종결/명사종결 (~음, ~함) |
| 본문 | 합쇼체 (~합니다, ~입니다) |

---

### 10. 한국어 마침표 줄바꿈

- \`한국어 문장.\` 뒤 공백+한국어 → 줄바꿈
- **제외:** 표 내부, 코드블록, 링크

---

### 11. 보충자료 출처 제한

| 허용 | 금지 |
|:---|:---|
| 공식 문서, 위키백과 | 논문 인용 |
| 공식 블로그, GDC Vault | 유튜브 링크/인용 |

---

### 12. Mermaid 다이어그램

- 허용: \`flowchart LR/TD\`, \`sequenceDiagram\`, \`gantt\`
- 금지: \`stateDiagram-v2\`
- 노드 10개 이하, 괄호 \`()\` 금지

| 제약 | 규칙 |
|:---|:---|
| 노드 수 | 10개 이하 |
| 노드명 | 한국어, 괄호 \`( )\` 금지 |
| 공백 포함 노드명 | \`["한글 이름"]\` 형식 |
| 같은 타입 반복 | 동일 탭 내 금지 |

---

### 13. 탭별 규칙 분리

| 탭 | 포함 | 제외 |
|:---|:---|:---|
| 기본학습/심화학습 | 개요, 목표, 핵심내용 | 실습, 평가, 강사가이드 |
| 기본실습/심화실습 | 실습 과제, 평가기준 | 이론 최소화 |
| 학습이해도 | 5지선다 10문제 + 채점 | 이미지태그, Mermaid |

---

### 코드 문제 템플릿

코딩 문제를 출제할 때는 반드시 아래 형식을 따릅니다:
- 문제 제목 / 문제 설명 / 입력 형식 / 출력 형식 / 예제 입출력 / 힌트

**규칙:**
- 예제 입력/출력은 반드시 **코드 블록**으로 감쌀 것
- 입력/출력 형식은 **구체적 제약조건**(범위, 타입, 크기) 명시
- 힌트는 정답을 직접 알려주지 않고 **사고 방향을 유도**
- 난이도 표기: 문제 제목 옆에 \`(난이도: ★☆☆)\` 형식 권장

---

### 평가 기준 (100점 루브릭)

| 등급 | 점수 범위 | 설명 |
|:---:|:---:|:---|
| S | 90~100 | 핵심 개념 완벽 이해 + 응용 가능 |
| A | 80~89 | 핵심 개념 이해, 일부 응용 |
| B | 70~79 | 기본 개념 이해, 응용 미흡 |
| C | 60~69 | 부분적 이해, 보충 필요 |
| F | 0~59 | 미이해, 재학습 필요 |
`;
}

function showConventionModal() {
    const html = marked.parse(getConventionMarkdown());
    showModal('📖 교안 컨벤션 규칙', html);
}
